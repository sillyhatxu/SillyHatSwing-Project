<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sillyhat.learningenglish.business.learningplan.mapper.LearningPlanMapper">

    <select id="queryUserLearningPlanByUserId" parameterType="java.lang.Long" resultType="com.sillyhat.learningenglish.business.personalinformation.dto.UserLearningPlanDTO">
        SELECT
            ID AS id,
            USER_ID AS userId,
            WORD_ID AS wordId,
            IS_LEARNING AS isLearning,
            REVIEW_FREQUENCY AS reviewFrequency,
            ERROR_FREQUENCY AS errorFrequency,
            SUCCESS_FREQUENCY AS successFrequency,
            (select USER_NAME from T_USER u where u.ID = t.CREATED_USER) AS createdUserName,
            CREATED_DATE AS createdDate,
            (select USER_NAME from T_USER u where u.ID = t.UPDATED_USER) AS updatedUserName,
            UPDATED_DATE AS updatedDate
        FROM
            T_USER_LEARNING_PLAN t
        WHERE
            t.USER_ID = #{userId}
    </select>

    <select id="queryUserLearningPlanAll" resultType="com.sillyhat.learningenglish.business.personalinformation.dto.UserLearningPlanDTO">
        SELECT
            ID AS id,
            USER_ID AS userId,
            WORD_ID AS wordId,
            IS_LEARNING AS isLearning,
            REVIEW_FREQUENCY AS reviewFrequency,
            ERROR_FREQUENCY AS errorFrequency,
            SUCCESS_FREQUENCY AS successFrequency,
            (select USER_NAME from T_USER u where u.ID = t.CREATED_USER) AS createdUserName,
            CREATED_DATE AS createdDate,
            (select USER_NAME from T_USER u where u.ID = t.UPDATED_USER) AS updatedUserName,
            UPDATED_DATE AS updatedDate
        FROM
            T_USER_LEARNING_PLAN t
    </select>


    <insert id="addUserLearningPlan" parameterType="com.sillyhat.learningenglish.business.personalinformation.dto.UserLearningPlanDTO">
        insert into T_USER_LEARNING_PLAN
        (
          USER_ID,
          WORD_ID,
          IS_LEARNING,
          REVIEW_FREQUENCY,
          ERROR_FREQUENCY,
          SUCCESS_FREQUENCY,
          CREATED_USER,
          CREATED_DATE,
          UPDATED_USER,
          UPDATED_DATE
        )
        values(
          #{userId},#{wordId},#{isLearning},#{reviewFrequency},#{errorFrequency},#{successFrequency},#{createdUser},datetime('now'),#{updatedUser},datetime('now')
        )
    </insert>

    <update id="updateUserLearningPlan" parameterType="com.sillyhat.learningenglish.business.personalinformation.dto.UserLearningPlanDTO">
        update T_USER_LEARNING_PLAN
          set
            USER_ID = #{userId},
            WORD_ID = #{wordId},
            IS_LEARNING = #{isLearning},
            REVIEW_FREQUENCY = #{reviewFrequency},
            ERROR_FREQUENCY = #{errorFrequency},
            SUCCESS_FREQUENCY = #{successFrequency},
            UPDATED_USER = #{updatedUser},
            UPDATED_DATE = datetime('now')
        where ID = #{id}
    </update>

</mapper>
